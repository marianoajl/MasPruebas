{# extending #}
{% extends "layout/_home-layout-no-menu.njk" %}
<!-- Main CSS file por ahora lo ponemos aca pero tenemos que hacer un condicional en el layout creo, 
veamos, porque desarrollo también tiene que cargar los estilos de cada página fuera del layout-->

{# import macros #}
{% import "macros/_button.njk" as btn %}
{% import "macros/_button-anchor.njk" as btnLink %}

{# Bloque de estilos especificos de éstá página #}
{% block css %}
    <link rel="stylesheet" type="text/css" media="screen" href="content/css/view5.css">
{% endblock %}

{% block content %}
    <section class="section">
        <article class="view">
            <h6 class="assistance__name">asistencia médica</h6>
            <h4 class="assistance__question">¿Dónde te encuentras?</h4>
            <form action="/view5.html" method="get" class="assistance__form needs-validation" novalidate>
                <section class="assistance__event">
                    <input type="radio" name="travelGroup" id="1"/>
                    <label for="1">Passenger #1</label>
                    <input type="radio" name="travelGroup" id="2"/>
                    <label for="2">Passenger #2</label>
                    <input type="radio" name="travelGroup" id="3"/>
                    <label for="3">Passenger #3</label>
                    <input type="radio" name="travelGroup" id="4"/>
                    <label for="4">Passenger #4</label>
                    <input type="radio" name="travelGroup" id="5"/>
                    <label for="5">Passenger #5</label>
                    <input type="radio" name="travelGroup" id="6"/>
                    <label for="6">Passenger #6</label>
                </section>
                <button type="submit" class="btn btn-outline-primary">continuar</button>
            </form>
        </article>
    </section>
{% endblock %}

{% block content2 %}
{% endblock %}

{% block js %}
    {# Bloque de estilos especificos de éstá página #}
    <script>
        $('.js-select').select2(
                {
                    //width: 100%,
                    minimumResultsForSearch: Infinity
                }
            );
    </script>
    <script>
        // Example starter JavaScript for disabling form submissions if there are invalid fields
    (function () {
        'use strict'

        // Fetch all the forms we want to apply custom Bootstrap validation styles to
        var forms = document.querySelectorAll('.needs-validation')

        // Loop over them and prevent submission
        Array.prototype.slice.call(forms)
        .forEach(function (form) {
            form.addEventListener('submit', function (event) {
                if (!form.checkValidity()) {
                    event.preventDefault()
                    event.stopPropagation()
                }

                form.classList.add('was-validated')
            }, false)
        })
    })()
    </script>
{% endblock %}